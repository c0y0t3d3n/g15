# G15
Logitech g15 utilities patched to work on current Debian

bash install.sh will install prerequisites, clone g15stats and g15macro repos, patch, build, and install
needs sudo, enter password when prompted
